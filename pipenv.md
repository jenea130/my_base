pipenv graph --reverse


# путь до виртуальной среды
pipenv --venv 


# путь до проекта
pipenv --where


---------------
# для переноса
pipenv lock

# в новом каталоге
pipenv install --ignore-pipfile
----------------
